#include <iostream>

using namespace std;

struct cate{

 int year;
 int cates;
 string events;

};
const int MaxN=100;

int main()
{
    int N,i;
    int Cnt=0;
    int index;
    cout<<"the count of events is ;";
    cin>>N;
    cout<<"the count of categories is:";
    cin>>cc;
    for(index=1;index<=N;index++){
    cin>>cate[index].year>>cate[index].cates>>cate[index].events;
    }

       string categories;
        for(i=1;i<=cc,i++){
                cin>>categories[i];
        if(cate[index].events == 0)
        cnt++;
        categories[Cnt]=i;
        cout<<categories[i]<<endl;
        return 0 ;
}



}
